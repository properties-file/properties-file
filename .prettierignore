# @see https://prettier.io/docs/ignore
# Prettier ignores `node_modules` and dot-files by default.

# Distribution (compiled code)
/dist

# Downlevel transform shadow tree (see src/build-scripts/downlevel/)
/.transformed-src

# Jest
/coverage

# Generated by tests/functional/generate.ts. Excluded so a repo-wide
# `prettier --write .` (run by `npm run build`) can't reformat it independently
# of the generator — that would produce needless diff noise on every build,
# since re-running the generator always emits raw (non-prettified) JSON.
/tests/functional/golden.json
